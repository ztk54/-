---
tags:
  - 数组
  - 函数
  - C语言
  - rand函数
  - 分支语句
时间: 2026-03-02T22:34:00
---
> 本文将以小白的视角完成c语言中经典项目扫雷游戏的实现，尽可能做到通俗易懂。

# 总体实现逻辑
我们将用maingame.c，game.c，game.h三部分实现游戏功能
1. maingame.c这是我们功能实现的主要逻辑，我们将要在里面实现玩家多次选择游玩的功能。
2. game.c这部分是我们函数功能的实现，其中会包括游戏的核心功能。
3. game.h我们将在maingame.c里面调用`#include "game.h"`以实现函数功能。
# 具体功能实现
1. maingame.c部分
	  * 我们的整体思路是在这里使用一个do while循环加switch嵌套使用来实现玩家的多次游玩。
	  * 首先定义一个变量intput，在do while循环中输入intput作为一个特征值，通过这个特征值和switch语句来实现玩家的选择功能，在这里我们希望玩家输入1进行游戏，输入0退出游戏，同时这里的intput也可以作为do while循环的判断语句，1和非零数字会继续循环，0则会退出循环。
	  * 具体代码功能实现如下：
	  * ```c
	    #include "game.h"
		int main()
		{
			int input = 0;
			do
			{
				menu();
				scanf("%d", &input);
				switch (input)
				{
				case 1:
					game();
					break;
				case 0:
					printf("退出游戏\n");
					break;
				default:
					printf("输入有错，请您重新输入");
					break;
				}
			} while (input);
		}
	    ```
2. game.h部分
	  * 在这里我们不妨来思考一下game.c大概由几部分组成：
		  * 开始菜单： menu
		  * 初始化雷：InitBoard
		  * 展示雷的位置：display_board
		  * 随机布置雷：set_my_mine
		  * 排查该地区是否是雷，不是雷显示周围一圈雷的个数：Find_Mine
		  * 获得周围一圈雷的个数：get_my_mine
	* 一些注意事项：
		* 我们可以在game.h里面包含后续使用的各种头文件以及各种宏：
			* 由于后续会各种使用行和列这两个变量我们就可以为二者进行宏定义：
			`#define ROW 9`
			 `#definr RANK 9`
			* 在这里我们需要思考一个问题，在查找周围一圈雷的个数的时候，边界会发生数组越界的情况，所以在实际上创建数组的时候我们要把数组的行和列扩大两个，防止数组发生越界情况，因此我们要再定义两个宏。
				`#define ROWS ROW+2`
				`#define RANKS ROW+2`
		* 具体代码实现如下：
		* ```c
		    #define _CRT_SECURE_NO_WARNINGS
			#pragma once
			#include <stdio.h>
			#include <stdlib.h>
			#include <time.h>
			
			#define ROW 9
			#define RANK 9
			#define ROWS ROW+2
			#define RANKS ROW+2
			#define COUNT 10
			
			void menu();
			void game();
			void InitBoard(char board[ROWS][RANKS], int row, int rank, char ch);
			void display_board(char board[ROWS][RANKS], int row, int rank);
			void set_my_mine(char board[ROWS][RANKS], int x, int y);
			void Find_Mine(char mine[ROWS][RANKS], char show[ROWS][RANKS], int row, int rank);
			int get_my_mine(char mine[ROWS][RANKS], int x, int y);
		  ```
	3. game.c部分
		1. menu：
			这个没什么好讲解的，我们用printf打印一个简略的初始菜单即可
			实现代码如下：
			```c
			void menu()
			{
				printf("************************\n");
				printf("******1. 继续游戏********\n");
				printf("******0. 退出游戏********\n");
				printf("************************\n");
			}
			```
		2. 二维数组选择方面：
			* 我们可以选择int和char类型的二维数组，但是由于char类型的二维数组既可以打印字符’0‘和字符*，因此我们选择**char类型**的二维数组。
			* 我们需要创建两个二维数组，一个用来存放我们生成雷的位置，另一个用来向玩家进行展示使用，两个都建议使用char类型的二维数组。
		3. 初始化两个二维数组：
				我们要把两个二位数组分别进行初始化，我们使用for循环的二重循环即可正常初始化，但是两个二维数组初始化对象不同，使用我们在创建函数的时候也需要传递需要初始化的字符。
			```	c
		void InitBoard(char board[ROWS][RANKS], int rows, int ranks, char ch)
		{
				for (int i = 0;i < rows;i++)
				{
					for (int j = 0;j < ranks;j++)
					{
						board[i][j] = ch;
					}
				}
			}		
			```
		4. 展示雷的位置：
		我们想到使用for循环的二重嵌套来打印图形是个很容易的事情，不过这里面也有一点需要我们注意的事项，比如说我们需要去打印一个坐标来供玩家去选择排雷的位置，而且在这里传递数据的时候我们只需要传递ROW和RANK即可，在for循环中也**不要从0开始打印**，从1开始循环。
		具体代码实现如下：
		```c
				void display_board(char board[ROWS][RANKS], int row, int rank)
				{
					for (int i = 0;i <= rank;i++)
					{
						printf("%d ", i);
					}
					printf("\n");
					for (int i = 1;i <= row;i++)
					{
						printf("%d ", i);
						for (int j = 1;j <= rank;j++)
						{
							printf("%c ", board[i][j]);
						}
						printf("\n");
					}
					printf("\n");
				}
		```
		5. 随机化雷的位置：
			我们可以使用[[rand函数的使用，以及猜数字游戏的实现 |rand函数]]来进行随机化生成雷的位置，这里注意我们要在game函数里面进行随机化种子，即`srand((unsigned int)time(NULL))`，然后在我们随机化生成雷的函数里面使用rand函数进行随机化生成坐标。注意这里都要用字符’1‘和'0'。
			代码实现如下：
			```c
		void set_my_mine(char board[ROWS][RANKS], int row, int rank)
			{
				int count = COUNT;
				while (count)
				{
					int x = rand() % row + 1;
					int y = rand() % rank + 1;
					if (board[x][y] == '0')
					{
						board[x][y] = '1';
						count--;
					}
				}
			}				
			```
		6. 获得周围一圈雷的个数：
			这里我们的思路就是传递需要查找的坐标，计算其周围一圈字符之（因为只存在0和1字符，累加起来就是雷的个数），当然这里累加起来是字符之和，我们需要的是数字，所以需要再去减去8*’0‘，这样子我们就能得到周围一圈雷的个数了。
			具体代码实现如下：
			```c
			int get_my_mine(char mine[ROWS][RANKS], int x, int y)
{
	return mine[x - 1][y - 1] + mine[x][y - 1] + mine[x + 1][y - 1] + mine[x + 1][y] + mine[x + 1][y + 1] +
		mine[x][y + 1] + mine[x - 1][y + 1] + mine[x - 1][y] - 8 * '0';
}
			```
		7. 排查该地区是否是雷，不是雷显示周围一圈雷的个数：
		* 首先我们需要使用一个循环来实现用户多次排查的功能，既然是循环那么必然是有退出条件的，如果用户把所有非雷的地方排查完毕了，那么用户即为游戏胜利了，所以我们应该设置一个计数器win，每当用户排查到非雷的地方，那么计数器加一，当`win==row*rank-COUNT`时候玩家胜利，同时退出循环。
		* 其次我们要求玩家输入要检测的坐标位置，这时候我们要对玩家输入进行合法检测，一是输入不能超过范围，二是不能输入非法字符。
		* 当玩家输入的坐标是雷的时候，我们就退出循环，并且给与玩家最终的雷分布图。
		* 当玩家输入的坐标不是雷的时候，我们调用之前的`get_my_mine`函数，同时将展示数组中这个雷的位置更改为周围雷的个数（注意数组里面存放的是字符，我们得到的是int类型的整数，因此需要再加上字符'0'，转化为字符）
		* 具体代码实现如下：
		 ```c
		   void Find_Mine(char mine[ROWS][RANKS], char show[ROWS][RANKS], int row, int rank)
			{
				int x = 0;
				int y = 0;
				int win = 0;
				while (win < row * rank - COUNT)
				{
					int check = scanf("%d %d", &x, &y);
					printf("请输入您想要排查的坐标\n");
					if (check!= 2)
					{
						printf("输入非法，重新输入\n");
						while (getchar() != '\n');
						continue;
					}
					if (x >= 1 && x <= row && y >= 1 && y <= row)
					{
						if (mine[x][y] == '1')
						{
							printf("很抱歉，您被炸死了\n");
							display_board(mine, ROW, RANK);
							break;
						}
						else
						{
							if (show[x][y] == '*')
							{
								int r = get_my_mine(mine, x, y);
								show[x][y] = r + '0';
								display_board(show, ROW, RANK);
								win++;
							}
							else
							{
								printf("该地方被排查过，不能重新排查\n");
							}
						}
					}
					else
					{
						printf("输入错误，请您重新输入\n");
					}
				}
				if (win == row * rank - COUNT)
				{
					printf("恭喜你，排雷成功\n");
				}
			}
		 ```
		8. 最终组装：
		```c
		void game()
		{
			srand((unsigned int)time(NULL));
			char Mine[ROWS][RANKS];
			char show[ROWS][RANKS];
			InitBoard(Mine, ROWS, RANKS, '0');
			InitBoard(show, ROWS, RANKS, '*');
			set_my_mine(Mine, ROW, RANK);
			display_board(show, ROW, RANK);
			Find_Mine(Mine, show, ROW, RANK);
		}
		```
# 结语
按照上述流程，大家应该可以很好的完成扫雷游戏的实现，不过上述只是一些最基本的功能，我们可以实现一个选择难度功能，以及经典扫雷游戏中的清除功能（需要使用函数递归），后续我也会持续发布新的博客来不断完善这些功能，希望大家可以多多支持我。