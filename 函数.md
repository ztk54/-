---
tags:
  - 函数
  - C语言
时间: 2026-02-12T21:51:00
---
* 嵌套使用：在一个函数里面再次调用一个函数
* 链式访问
  > [!notes]
  > 这里举一个很有意思的例子
  > ```c
  > 		 printf("%d",printf("%d",printf("%d",43)));
  > ```
  
  首先，会先输出一个`43`，由于printf函数的返回值是**打印的个数**，所以第一次返回2，在屏幕上打印2，然后再次返回1，因此最终结果是`4321`。
  
  