这是学完[[函数]]之后一个进阶的功能
1. 使用递归顺序输出数字每一位
	核心思想是递归加回退
```c
	#define _CRT_SECURE_NO_WARNINGS
	#include <stdio.h>
	void Print(int n)
	{
		if (n > 9)
		{
			Print(n / 10);
		}
		printf("%d ", n % 10);
	}
	int main()
	{
		int n;
		scanf("%d", &n);
		Print(n);
	}
```
> [!tips]
> 这里我最开始的想法是有一定问题的，这里有递归和回退两个部分。假如说输入为1234，使用会不断调用Print函数直到n为1，这时候输出1，然后回退到上次递归（即n为12的时候，然后输出12%10的值，即2）依次类推。**函数执行是线性的，因此执行过的if判断语句在回退的时候不会再次进行判断**