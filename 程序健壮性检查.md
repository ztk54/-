1. ```c
   int check = scanf("%d %d", &x, &y);
	printf("请输入您想要排查的坐标\n");
	if (check!= 2)
	{
		printf("输入非法，重新输入\n");
		while (getchar() != '\n');
		continue;
	}
   ```
这里用scanf检查健壮性一定要使用getchar清空缓冲区，不然会造成程序死循环。